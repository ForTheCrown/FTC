package ftc.crownapi.settings;

import ftc.crownapi.Main;
import org.bukkit.configuration.ConfigurationSection;

public class CrownSettings {

    private static final ConfigurationSection settingSection = Main.plugin.getConfig().getConfigurationSection("settings");
    /*
    private static boolean arePointsCumulative;
    private static boolean teleportPlayersOnQuit;
    private static boolean removePlayersOnShutdown;
    private static boolean tpSpawnOnFinish;
     */

    public static ConfigurationSection getSettingSection() {
        return settingSection;
    }

    public static boolean getBooleanSetting(CrownBooleanSettings setting){
        String stringSetting = setting.toString().replace('_', '-');
        return settingSection.getBoolean(stringSetting.toLowerCase());
    }
    public static void setBooleanSetting(CrownBooleanSettings setting, boolean value){
        String stringSetting = setting.toString().replace('_', '-');
        settingSection.set(stringSetting.toLowerCase(), value);
    }

    /*
    public static boolean arePointsCumulative() {
        return arePointsCumulative;
    }
    public static void setArePointsCumulative(boolean value) {
        arePointsCumulative = value;
    }

    public static boolean getTpOnQuit() {
        return teleportPlayersOnQuit;
    }
    public static void setTpOnQuit(boolean value){
        teleportPlayersOnQuit = value;
    }

    public static boolean getRemoveOnShutdown() {
        return removePlayersOnShutdown;
    }
    public static void setRemoveOnShutdown(boolean value){
        removePlayersOnShutdown = value;
    }

    public static boolean getTpSpawnOnEnd(){
        return tpSpawnOnFinish;
    }
    public static void setTpSpawnOnEnd(boolean value){
        tpSpawnOnFinish = value;
    }
*/

    public static void reloadSettings() {
        Main.plugin.reloadConfig();
        /*
        arePointsCumulative = _getCumulativePoints();
        teleportPlayersOnQuit = _getTpPlayersOnQuit();
        removePlayersOnShutdown = _getRemovePlayersOnShutDown();
        tpSpawnOnFinish = _getTpSpawnOnFinish();
         */
    }

    public static void saveSettings() {
        /*
        getSettingSection().set("cumulative-points", arePointsCumulative());
        getSettingSection().set("teleport-on-quit", getTpOnQuit());
        getSettingSection().set("remove-players-on-shutdown", getRemoveOnShutdown());
        getSettingSection().set("tp-spawn-on-finish", getTpSpawnOnEnd());
         */
        Main.plugin.saveConfig();
    }

    /*
    //private getters, I'm literally copying how the Essentials UserData file is written with these private getters lol
    private static boolean _getCumulativePoints(){
        return getSettingSection().getBoolean("cumulative-points");
    }
    private static boolean _getTpPlayersOnQuit() {
        return getSettingSection().getBoolean("teleport-on-quit");
    }
    private static boolean _getRemovePlayersOnShutDown() {
        return getSettingSection().getBoolean("remove-players-on-shutdown");
    }
    private static boolean _getTpSpawnOnFinish(){
        return getSettingSection().getBoolean("tp-spawn-on-end");
    }
     */
}